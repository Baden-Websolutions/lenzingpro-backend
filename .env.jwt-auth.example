# JWT Authentication Environment Variables
# Add these to your existing .env file

# JWT Validation Configuration
# JWKS URI for JWT signature verification
JWT_JWKS_URI=https://fidm.eu1.gigya.com/oidc/op/v1.0/4_XQnjjmLc16oS7vqA6DvIAg/v2.0/keys

# JWT Issuer (must match 'iss' claim in JWT)
JWT_ISSUER=https://fidm.eu1.gigya.com/oidc/op/v1.0/4_XQnjjmLc16oS7vqA6DvIAg

# JWT Audience (must match 'aud' claim in JWT)
# Can use CDC_OIDC_CLIENT_ID if JWT is issued by CDC
JWT_AUDIENCE=ABbd672Koy3U

# Optional: JWT Clock Tolerance (in seconds)
# Allows for clock skew between systems
JWT_CLOCK_TOLERANCE=30

# Note: No additional Commerce Cloud configuration needed
# JWT auth flow reuses existing COMMERCE_* variables from .env
